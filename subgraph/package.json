{
  "name": "blocxtactoe-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ thebabalola/blocxtactoe-subgraph",
    "create-local": "graph create --node http://localhost:8020/ thebabalola/blocxtactoe-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ thebabalola/blocxtactoe-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 thebabalola/blocxtactoe-subgraph"
  },

  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.36.0"
  }
}
